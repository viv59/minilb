# public

Static files served as-is at the site root (favicon, robots.txt, manifest.json).
Nothing in here goes through Vite's build pipeline — for anything imported by
a component, use `src/assets/` instead.
