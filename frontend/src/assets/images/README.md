# images

Static raster/vector images (screenshots, illustrations, backgrounds)
that get referenced with a real `src=` — not decorative CSS patterns,
those live inline in the component (see `InfiniteGrid.jsx`).
