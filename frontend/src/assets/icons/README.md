# icons

Custom SVG/PNG icons that aren't covered by `lucide-react` (the icon set
used throughout the app) go here — e.g. a custom logo mark or brand glyphs.
Import them like: `import Logo from '@/assets/icons/logo.svg'`.
